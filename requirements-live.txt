# Live deployment only. The backtest needs none of this.
#
# ib_insync is unmaintained upstream since 2024, so both it and its event
# library are pinned: an incompatible eventkit or nest_asyncio release is the
# most likely way this deployment breaks without any code change.
ib_insync==0.9.86
eventkit==1.0.3
nest_asyncio==1.6.0

# Shared with the backtest, repeated here so the VM can install from this file
# alone without pulling jupyter and matplotlib onto a 2 GB box.
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
lxml>=4.9          # universe scrape (pd.read_html)
