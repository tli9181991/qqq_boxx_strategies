# Live deployment only. The backtest needs none of this.
#
# ib_async is the maintained community fork of ib_insync (which has been
# unmaintained upstream since 2024). Same API, still worth pinning: this is the
# layer that places orders, and it should only ever move when you decide it
# does. It pulls aeventkit, nest_asyncio and tzdata itself.
#
# Needs Python >= 3.10.
ib_async==2.1.0

# Shared with the backtest, repeated here so the VM can install from this file
# alone without pulling jupyter and matplotlib onto a 2 GB box.
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
lxml>=4.9          # universe scrape (pd.read_html)
